\* TLC Model Configuration for KernelInvariants.tla
\* Task 14.1 — TLA+ spec: kernel invariant state machine
\* Run: java -jar tla2tools.jar -config KernelInvariants.cfg KernelInvariants

SPECIFICATION Spec

CONSTANTS
    GATES = {"K1", "K2", "K3", "K4", "K5", "K6", "K7", "K8"}

INVARIANT
    SafetyInvariant

PROPERTY
    EnteringTerminates
    ActiveTerminates
    ExitingTerminates
    FaultedTerminates
    EventuallyIdle
